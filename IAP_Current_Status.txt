================================================
IAP JOURNALHUB — CURRENT IMPLEMENTATION STATUS
Scan Date: 2026-04-05
Codebase: D:\Kreadevs\web-journal
Purpose: Compare against IAP TRD / CLAUDE.md spec
================================================


[SECTION 1] USER ROLES & AUTHENTICATION
========================================

✅ Author self-signup (POST /api/auth/signup role=author)
✅ Reviewer self-signup (POST /api/auth/signup role=reviewer)
✅ Publisher self-signup (POST /api/auth/signup role=publisher)
✅ OTP request before account created (POST /api/auth/create — sends OTP to email)
✅ OTP verified before account created (POST /api/auth/verifysignup)
✅ Login with credentials (POST /api/auth/login)
✅ Login OTP verification (POST /api/auth/verifyLoginOTP)
✅ OTP resend (POST /api/auth/resend)
✅ JWT access token issued with { sub, active_role, active_journal_id, roles[] }
✅ Refresh token (POST /api/auth/token — httpOnly cookie)
✅ Logout (POST /api/auth/logout — deletes refresh token)
✅ Role switch (POST /api/auth/switch-role — validates user_roles table, returns new JWT)
✅ Staff creation (POST /api/auth/create-staff — authorized: chief_editor, publisher, journal_manager, sub_editor)
✅ OTP UPSERT: one OTP per email (ON CONFLICT DO UPDATE in otp.repository)
✅ OTP expiry: 10 minutes
✅ Password change (PUT /api/profile/change-password)
✅ Roles blocked from self-signup: owner, chief_editor, sub_editor, journal_manager
⚠️ Password reset flow (sendPasswordResetEmail exists in authEmails.ts but no /forgot-password or /reset-password endpoint in auth.route.ts)
⚠️ OTP dev bypass (auth.controller.ts skips OTP verification in dev mode — returns tokens directly)


[SECTION 2] PUBLISHER FEATURES
================================

✅ Create journal (POST /api/journal/publisherCreate — publisher only, includes logo upload)
✅ Journal fields: name, ISSN, DOI, publisher_name, type, peer_review_policy, oa_policy, author_guidelines, aims_and_scope, publication_fee, currency
✅ Auto-generate acronym from journal name (service layer)
✅ Unique ISSN validation (migration 038 adds unique constraint)
✅ Unique DOI validation (migration 038 adds unique constraint)
✅ Set APC per journal (PATCH /api/journal/:journalId/apc — publisher only)
✅ Upload journal logo (POST /api/journal/:id/logo — publisher, journal_manager)
✅ Create Chief Editor inline during journal creation (POST /api/auth/create-staff role=chief_editor)
✅ Create Journal Manager inline during journal creation (POST /api/auth/create-staff role=journal_manager)
✅ Welcome email to chief_editor/journal_manager on creation (sendWelcomeEmail)
✅ Take down journal (POST /api/publisher/journals/:journalId/takedown — publisher only)
✅ Restore journal (POST /api/publisher/journals/:journalId/restore — publisher only)
✅ Take down issue (POST /api/publisher/journals/:journalId/issues/:issueId/takedown)
✅ Restore issue (POST /api/publisher/journals/:journalId/issues/:issueId/restore)
✅ Take down paper (POST /api/publisher/papers/:paperId/takedown)
✅ Restore paper (POST /api/publisher/papers/:paperId/restore)
✅ View pending issue requests from JM (GET /api/journal-issue/pending-requests)
✅ Approve/reject issue requests (PUT /api/journal-issue/requests/:requestId/review)
✅ Publish accepted papers (PUT /api/publication/publishPaper/:paperId — publisher only)
✅ View pending payments (GET /api/payments/pending)
✅ Approve/reject payment receipts (POST /api/payments/paper/:paperId/approve)
✅ Send payment reminder (POST /api/payments/paper/:paperId/remind — publisher only)
✅ View all payments (GET /api/payments/all)
✅ View rejected payments (GET /api/payments/rejected)
✅ Get journal payments (GET /api/publisher/getJournalPayments)
✅ Publish issue (PUT /api/publisher/publishIssue/:issueId)
✅ Manual issue reset (POST /api/publisher/issues/reset-all — publisher only)
✅ Replace chief editor (PATCH /api/publisher/journals/:journalId/chief-editor — publisher only)
✅ Send invoice (POST /api/publisher/sendInvoice — publisher only)
✅ Publisher dashboard (/publisher — publisherDashboard.tsx)
✅ Create journal page (/publisher/create-journal — CreateJournal.tsx)
✅ Publisher payments page (/publisher/payments — Payments.tsx)
✅ Journal categories CRUD (/publisher/categories — Categories.tsx)
✅ Journal-level categories CRUD (/publisher/journal-categories — JournalCategories.tsx)
✅ Homepage content CMS (/publisher/homepage-content — HomepageContent.tsx, manages conferences)
✅ Upcoming conferences CRUD (GET/POST/DELETE /api/conferences)
⚠️ Journal renewal payment frontend (journal_payments table + sendJournalExpiryInvoiceEmail exist, but no /publisher/journal-renewals page; yearlyEmailCron COMMENTED OUT)
⚠️ GET /api/publisher/papers/:issueId — lacks explicit authorize() guard (only authMiddleware)
⚠️ POST /api/publisher/sendEmail — lacks explicit authorize() guard (only authMiddleware)
❌ PUT /api/publisher/approve/:paymentId — NO authMiddleware AND NO authorize() guard (fully public endpoint)


[SECTION 3] JOURNAL MANAGEMENT (Role: Journal Manager)
========================================================

✅ Journal Manager dashboard (/publisher-manager — PublisherManagerDashborad.tsx)
✅ View published papers for journal (GET /api/journal-issue/my-papers — journal_manager only)
✅ View issues for journal (GET /api/journal-issue/my-issues — journal_manager only)
✅ Filter issues by status (all/open/closed/draft/published)
✅ Request new issue (POST /api/journal-issue/request — journal_manager only)
✅ View own issue requests (GET /api/journal-issue/:journalId/requests — journal_manager only)
✅ Next issue preview (GET /api/journal-issue/:journalId/next-issue-preview)
✅ Sidebar nav: Dashboard, All Issues, Archive, Papers, Editorial Board, Publication Ethics (defined in roles.ts)
✅ Editorial Board tab (/publisher-manager?tab=editorial — implemented in PublisherManagerDashborad.tsx, fetches GET /api/journal/:id/editorial-board)
✅ Publication Ethics tab (/publisher-manager?tab=ethics — implemented in PublisherManagerDashborad.tsx, fetches peer_review_policy + oa_policy)
✅ URL sync for tabs (?tab= query param synced with useSearchParams)
✅ Request New Issue dialog with next-issue preview
✅ "Publish Paper" route removed from JM access (publisher-only in App.tsx)
⚠️ JM cannot assign paper to issue from UI (PATCH /api/papers/:paperId/assign-issue allows journal_manager but no JM UI for this)
⚠️ Issue request notification email to publisher (endpoint triggers insert but no email function called on issue request)


[SECTION 4] JOURNAL ISSUES
============================

✅ Create issue (POST /api/journal-issue/addJournalIssue/:journalId — owner, publisher, journal_manager)
✅ List issues for journal (GET /api/journal-issue/getJournalIssues/:journalId)
✅ Update issue (PUT /api/journal-issue/updateJournalIssue/:issueId)
✅ Delete issue (DELETE /api/journal-issue/deleteJournalIssue/:issueId)
✅ Issue status: open/closed/draft/published (enum in DB)
✅ "Open for Call to Paper" button in CE journal detail (PUT /api/chiefEditor/updateIssueStatus/:issueId)
✅ "Close Submissions" button in CE journal detail
✅ Open/closed status badge on issue cards (CEJournalDetail.tsx)
✅ Issue request from JM to Publisher (POST /api/journal-issue/request)
✅ Publisher approves/rejects issue request (PUT /api/journal-issue/requests/:requestId/review)
✅ Auto yearly issue reset cron (issueResetCron — ACTIVE, runs Jan 1 at 00:01)
✅ Next issue preview (GET /api/journal-issue/:journalId/next-issue-preview)
✅ Takedown/restore issues (publisher endpoints)
❌ PUT /api/chiefEditor/updateIssueStatus/:issueId — NO authMiddleware, NO authorize() guard (publicly accessible)
❌ POST /api/chiefEditor/assignPaperToIssue — NO authMiddleware, NO authorize() guard (publicly accessible)
❌ GET /api/chiefEditor/getPapersByIssue/:issueId — NO authMiddleware, NO authorize() guard (publicly accessible)
⚠️ journalSuspensionCron — COMMENTED OUT (journals past expiry_at not auto-suspended)


[SECTION 5] PAPER SUBMISSION (Author)
======================================

✅ Paper submission form (/author/submit — SubmitPaper.tsx)
✅ Select journal dropdown (shows only journals with open issues)
✅ Title field (max 200 chars with counter)
✅ Author names (repeatable, + Add button)
✅ Corresponding authors (repeatable, max 5)
✅ Keywords (tag/chip input, max 5)
✅ Keyword suggestions from existing papers (GET /api/papers/keyword-suggestions)
✅ References (repeatable, max 5, with link field)
✅ Upload manuscript (.docx and .tex/.latex only, max 10MB — manuscriptUpload middleware)
✅ Review modal before final submission (confirmation dialog)
✅ Submit button triggers review modal
✅ POST /api/papers/createPaper — sets paper.status = 'submitted'
✅ paper_status_log row inserted on submission
✅ Submission confirmation email to author (sendSubmissionConfirmationEmail)
✅ Extract metadata from uploaded file (POST /api/papers/extract-metadata)
✅ Policies acceptance checkbox (apc_agreed field in DB — migration 045)
✅ Track paper (/author/track/:paperId — TrackPaper.tsx)
✅ Upload revision (POST /api/papers/:paperId/revision)
✅ Version history (GET /api/papers/:paperId/versions — PaperVersions.tsx)
✅ View author's submissions (GET /api/papers/getPapersByAuthor)
⚠️ MySubmissions.tsx page exists but route is commented out in App.tsx (using AuthorDashboard instead)
⚠️ Journal selection for submission uses /api/author/getAuthorJournals — only shows journals where author has open issues, not all open journals


[SECTION 6] PEER REVIEW WORKFLOW
==================================

✅ CE assigns paper to Associate Editor (POST /api/chiefEditor/assignSubEditor/:paperId — chief_editor only)
✅ CE replaces Associate Editor (POST /api/chiefEditor/papers/:paperId/replace-ae — chief_editor only)
✅ AE accepts/declines assignment (PUT /api/editorAssignment/handleAssignmentStatus/:editorAssignmentId — sub_editor only)
✅ AE views assigned papers (GET /api/subEditor/getSubEditorPapers — sub_editor only)
✅ AE updates paper status (PUT /api/subEditor/updateSubEditorPaperStatus/:paperId — sub_editor only)
✅ AE searches reviewers (GET /api/subEditor/fetchReviewer — sub_editor only)
✅ AE assigns reviewer (POST /api/subEditor/assignReviewer/:paperId — sub_editor, chief_editor)
✅ AE invites reviewer (POST /api/subEditor/inviteReviewer — sub_editor only)
✅ AE suggests reviewer for CE approval (POST /api/subEditor/suggestReviewer/:paperId — sub_editor only)
✅ CE reviews pending reviewer suggestions (PUT /api/subEditor/reviewer-requests/:requestId/review — chief_editor only)
✅ CE views pending reviewer requests (GET /api/subEditor/pending-reviewer-requests — chief_editor only)
✅ AE views reviews for paper (GET /api/subEditor/getReviewsForPaper/:paperId — sub_editor, chief_editor)
✅ AE views reviewers for paper (GET /api/subEditor/getReviewersForPaper/:paperId — sub_editor, chief_editor)
✅ Reviewer assignment notification email (sendReviewerAssignmentEmail)
✅ Reviewer views assigned papers (GET /api/reviewer/getReviewerPapers — reviewer only)
✅ Reviewer submits review (POST /api/reviewer/submitReview/:paperId — reviewer only, with signature upload)
✅ Review submitted via review module (POST /api/review/submitReview/:reviewAssignmentId — reviewer only)
✅ Review submission confirmation email (sendReviewSubmissionConfirmationEmail)
✅ Reviewer reminder email (sendReviewerReminderEmail)
✅ CE sends reminder to specific reviewer (POST /api/chiefEditor/papers/:paperId/remind-reviewer/:reviewerId)
✅ CE sends reminder to all reviewers on paper (POST /api/chiefEditor/papers/:paperId/remind-reviewer)
✅ CE sends bulk reminder to reviewer (POST /api/chiefEditor/reviewer/:reviewerId/remind)
✅ CE sends reminder to AE (POST /api/chiefEditor/papers/:paperId/remind-ae)
✅ CE sends bulk reminder to AE (POST /api/chiefEditor/ae/:aeId/remind)
✅ Get sub-editor assignments (GET /api/reviewAssignment/getSubEditorAssignments — sub_editor only)
✅ CE get submitted reviews (GET /api/chiefEditor/getSubmittedReviews — chief_editor only)
✅ ReviewerMatrix component (ReviewerMatrix.tsx)
✅ Reviewer applications (GET /api/contact/apply-reviewer — public, sends emails)
✅ CE views reviewer applications (GET /api/chiefEditor/applications, GET /api/chiefEditor/applications/count)
✅ CE invites applicant (POST /api/chiefEditor/applications/:applicationId/invite)
✅ CE declines applicant (POST /api/chiefEditor/applications/:applicationId/decline)
⚠️ Reviewer decline flow (no explicit "decline review" endpoint found; AE can see assignments only)


[SECTION 7] EDITORIAL DECISIONS
==================================

✅ AE makes decision (POST /api/subEditor/decision/:paperId — sub_editor only)
✅ AE existing decision check (GET /api/subEditor/existingDecision/:paperId — sub_editor only)
✅ CE final decision (POST /api/chiefEditor/decide/:paperId — chief_editor only, with validation schema)
✅ CE override paper status (POST /api/chiefEditor/papers/:paperId/override-status — chief_editor only)
✅ CE update paper status (PUT /api/chiefEditor/updatePaperStatus/:paperId — chief_editor only)
✅ CE decision history (GET /api/chiefEditor/papers/:paperId/decision-history — chief_editor, sub_editor, author)
✅ Editorial decision email to author (sendEditorialDecisionEmail)
✅ Revision request email (sendRevisionRequestEmail)
✅ Acceptance notice email (sendAcceptanceNoticeEmail)
✅ CE accepted_at timestamp set when decision = 'accepted' in editor_decisions
✅ sub_editor_decisions table records AE decisions (migration 041)
✅ Reviewed papers view for CE (/chief-editor/accepted — ReviewedPapers.tsx)
✅ CE views papers by journal (GET /api/chiefEditor/getPapers/:journalId — chief_editor only)
✅ CE views all papers (GET /api/chiefEditor/getAllPapers — chief_editor only)


[SECTION 8] PAYMENT SYSTEM
============================

✅ Paper payment table (paper_payments in DB — migration 013)
✅ Author uploads payment receipt (POST /api/payments/paper/:paperId/upload-receipt — author only)
✅ Publisher views pending payments (GET /api/payments/pending — publisher only)
✅ Publisher views all payments (GET /api/payments/all — publisher only)
✅ Publisher views rejected payments (GET /api/payments/rejected — publisher only)
✅ Publisher approves/rejects receipt (POST /api/payments/paper/:paperId/approve — publisher only)
✅ Get payment for a paper (GET /api/payments/paper/:paperId — authMiddleware, any role)
✅ Payment approved notification (sendPaymentApprovalEmail)
✅ Payment rejected notification (sendPaperPaymentEmail)
✅ Receipt notification email (sendReceiptNotificationEmail)
✅ Payment reminder email (sendPaymentReminderEmail)
✅ Send payment reminder (POST /api/payments/paper/:paperId/remind — publisher only, 24hr rate limit via paper_reminders table)
✅ Journal payment table (journal_payments — migration 012)
✅ Journal expiry invoice (sendJournalExpiryInvoiceEmail exists)
✅ Invoice email (sendInvoiceEmail — used in publisher.sendInvoice)
✅ Paper payment status tracking in TrackPaper.tsx
✅ APC (apc_agreed field on papers — migration 045)
⚠️ Payment flow: awaiting_payment / payment_review statuses exist in DB enum and StatusBadge but paper workflow may skip them depending on APC setting
❌ PUT /api/publisher/approve/:paymentId — NO authMiddleware, NO authorize() guard (fully public, security bug)
❌ Journal renewal payment frontend (no /publisher/journal-renewals page; yearlyEmailCron COMMENTED OUT)


[SECTION 9] PUBLICATION & DOI
===============================

✅ Publish paper (PUT /api/publication/publishPaper/:paperId — publisher only)
✅ published_at set automatically when paper inserted into publications (service layer)
✅ paper_index computed per issue on publish (MAX(paper_index)+1)
✅ url_slug generated on publish (format: {doi-prefix}-{acronym}-{year}-{issue_index}-{paper_index} — migration 047)
✅ DOI suggestion (GET /api/papers/:paperId/suggest-doi — publisher only)
✅ Metadata check before publish (GET /api/papers/:paperId/metadata-check — publisher, chief_editor, journal_manager)
✅ Publication confirmation email (sendPublicationConfirmationEmail)
✅ DOI registration email function (sendDOIRegistrationEmail — exists in emails/)
✅ Proof availability email function (sendProofAvailabilityEmail — exists in emails/)
✅ Public article page with slug URL format (/:acronym/:slug — ArticlePage.tsx)
✅ Backward compat redirect (/articles/:paperId — ArticleRedirect.tsx fetches slug then redirects)
✅ getPaperUrl() helper in client/src/lib/utils.ts (uses slug if available, falls back to /articles/:id)
✅ All link locations updated to use getPaperUrl (PaperDetail, Archive, AuthorDashboard, TrackPaper, LandingPage, ArticlePage)
✅ published_at displayed on public article page (ArticlePage.tsx)
✅ published_at displayed on author tracking page (TrackPaper.tsx)
✅ GET /api/publication/getSubmittedReviews (journal_manager, publisher)
✅ On-demand HTML rendering for manuscript (POST /api/papers/:paperId/html — sub_editor, reviewer, chief_editor, author, publisher)
✅ Public HTML view (GET /api/browse/paper/:paperId/html — no auth)
✅ Get paper by slug (GET /api/browse/article/:acronym/:slug — no auth)
✅ Get paper slug by ID (GET /api/browse/paper/:paperId/slug — no auth)
⚠️ sendDOIRegistrationEmail — function exists but no controller found calling it; DOI registration email never sent
⚠️ sendProofAvailabilityEmail — function exists but no controller found calling it; proof availability email never sent
⚠️ HTML conversion (on-demand via LibreOffice/pandoc in browse.service.ts; no automated pipeline)


[SECTION 10] CHIEF EDITOR FEATURES
=====================================

✅ CE dashboard (/chief-editor — ChiefEditorDashboard.tsx)
✅ CE journals list (/chief-editor/journals — CEJournals.tsx)
✅ CE journal detail with issues (/chief-editor/journals/:journalId — CEJournalDetail.tsx)
✅ CE papers management (/chief-editor/papers — CEPapers.tsx)
✅ CE team management (/chief-editor/team — CETeam.tsx)
✅ CE applications management (/chief-editor/applications — CEApplications.tsx)
✅ CE stats (/chief-editor/stats — CEStats.tsx)
✅ CE reviewed/accepted papers (/chief-editor/accepted — ReviewedPapers.tsx)
✅ CE get journal details (GET /api/chiefEditor/journals/:journalId/details — chief_editor only)
✅ CE get journals (GET /api/chiefEditor/getChiefEditorJournals — chief_editor only)
✅ CE get papers by journal (GET /api/chiefEditor/getPapers/:journalId — chief_editor only)
✅ CE get all papers (GET /api/chiefEditor/getAllPapers — chief_editor only)
✅ CE assign AE to paper (POST /api/chiefEditor/assignSubEditor/:paperId — chief_editor only)
✅ CE replace AE (POST /api/chiefEditor/papers/:paperId/replace-ae — chief_editor only)
✅ CE final decision (POST /api/chiefEditor/decide/:paperId — chief_editor only)
✅ CE update paper status (PUT /api/chiefEditor/updatePaperStatus/:paperId — chief_editor only)
✅ CE override paper status (POST /api/chiefEditor/papers/:paperId/override-status — chief_editor only)
✅ CE decision history (GET /api/chiefEditor/papers/:paperId/decision-history)
✅ CE get submitted reviews (GET /api/chiefEditor/getSubmittedReviews — chief_editor only)
✅ CE invite sub-editor (POST /api/chiefEditor/inviteSubEditor — chief_editor only)
✅ CE fetch chief editors (GET /api/chiefEditor/getChiefEditors — author, admin, owner, publisher)
✅ CE fetch sub editors (GET /api/chiefEditor/getSubEditors — author, chief_editor, owner, publisher)
✅ CE fetch reviewers (GET /api/chiefEditor/getReviewers — chief_editor, sub_editor)
✅ CE stats (GET /api/chiefEditor/stats — chief_editor only)
✅ CE send reminder to AE (POST /api/chiefEditor/papers/:paperId/remind-ae)
✅ CE send reminder to specific reviewer (POST /api/chiefEditor/papers/:paperId/remind-reviewer/:reviewerId)
✅ CE send reminder to all reviewers (POST /api/chiefEditor/papers/:paperId/remind-reviewer)
✅ CE bulk remind AE (POST /api/chiefEditor/ae/:aeId/remind)
✅ CE bulk remind reviewer (POST /api/chiefEditor/reviewer/:reviewerId/remind)
✅ CE applications list + count (GET /api/chiefEditor/applications, /applications/count)
✅ CE invite/decline applicant (POST /api/chiefEditor/applications/:id/invite, /decline)
✅ submitted_at, accepted_at, published_at displayed on CE paper view (CEPapers.tsx)
✅ PaperTimeline in CE paper detail dialog (fetches GET /api/papers/:id/status-log)
✅ "Open for Call to Paper" / "Close Submissions" button in CEJournalDetail.tsx
❌ GET /api/chiefEditor/getPapersByIssue/:issueId — NO auth guard (publicly accessible)
❌ POST /api/chiefEditor/assignPaperToIssue — NO auth guard (publicly accessible)
❌ PUT /api/chiefEditor/updateIssueStatus/:issueId — NO auth guard (publicly accessible)


[SECTION 11] ASSOCIATE EDITOR (Sub-Editor) FEATURES
=====================================================

✅ AE dashboard (/sub-editor — SubEditorDashboard.tsx)
✅ AE revision paper page (/sub-editor/revision — RevisionPaper.tsx)
✅ AE view assigned papers (GET /api/subEditor/getSubEditorPapers — sub_editor only)
✅ AE update paper status (PUT /api/subEditor/updateSubEditorPaperStatus/:paperId — sub_editor only)
✅ AE search reviewers (GET /api/subEditor/fetchReviewer — sub_editor only)
✅ AE assign reviewer (POST /api/subEditor/assignReviewer/:paperId — sub_editor, chief_editor)
✅ AE invite reviewer (POST /api/subEditor/inviteReviewer — sub_editor only)
✅ AE suggest reviewer (POST /api/subEditor/suggestReviewer/:paperId — sub_editor only)
✅ AE view reviewers for paper (GET /api/subEditor/getReviewersForPaper/:paperId)
✅ AE view reviews for paper (GET /api/subEditor/getReviewsForPaper/:paperId)
✅ AE make decision (POST /api/subEditor/decision/:paperId — sub_editor only)
✅ AE existing decision check (GET /api/subEditor/existingDecision/:paperId — sub_editor only)
✅ AE accept/decline assignment (PUT /api/editorAssignment/handleAssignmentStatus/:id — sub_editor only)
✅ AE view own assignments (GET /api/editorAssignment/getReviews — sub_editor only)
✅ AE view sub-editor assignments (GET /api/reviewAssignment/getSubEditorAssignments — sub_editor only)
✅ PaperTimeline tab in SubEditorDashboard.tsx (fetches GET /api/papers/:id/status-log)
✅ Editor assignment notification email (sendEditorAssignmentEmail)
✅ Sub-editor invite email (sendSubEditorInviteEmail)


[SECTION 12] REVIEWER FEATURES
=================================

✅ Reviewer dashboard (/reviewer — ReviewerDashboard.tsx)
✅ Reviewer completed reviews (/reviewer/completed — completedReview.tsx)
✅ Reviewer review detail (/reviewer/completed/:paperId — ReviewDetail.tsx)
✅ Reviewer view assigned papers (GET /api/reviewer/getReviewerPapers — reviewer only)
✅ Reviewer submit review with signature (POST /api/reviewer/submitReview/:paperId — reviewer only)
✅ Submit review via review module (POST /api/review/submitReview/:reviewAssignmentId — reviewer only)
✅ Signature capture modal (SignatureModal.tsx component)
✅ Reviewer application system (POST /api/contact/apply-reviewer — public, with profile pic upload)
✅ Reviewer application confirmation email (sendReviewerApplicationConfirmation)
✅ Reviewer application sent to editor email (sendReviewerApplicationToEditor)
✅ Reviewer rejection email (sendRejectionEmailToApplicant)
✅ Reviewer invitation (sent via invitation system by AE/CE)
✅ Reviewer invite email (sendReviewerInviteEmail)
✅ Reviewer assignment notification (sendReviewerAssignmentEmail)
✅ Reviewer reminder email (sendReviewerReminderEmail)
✅ ReviewerMatrix component (ReviewerMatrix.tsx — visible in CE/AE views)
✅ Profile management (PUT /api/profile/updateProfile, GET /api/profile/getProfile)
✅ Certification upload (POST /api/profile/certifications)
✅ View certifications (GET /api/profile/certifications)
✅ Delete certification (DELETE /api/profile/certifications/:certId)
⚠️ Reviewer decline flow (no explicit "decline assigned paper" endpoint)
⚠️ Reviewer cannot edit submitted review (no PUT /review endpoint found)


[SECTION 13] AUTHOR FEATURES
===============================

✅ Author dashboard (/author — AuthorDashboard.tsx — two panels: Recent Submissions + Published Articles)
✅ Submit paper (/author/submit — SubmitPaper.tsx with full form)
✅ Track paper (/author/track/:paperId — TrackPaper.tsx with full status + dates)
✅ Paper versions (/author/version — PaperVersions.tsx)
✅ View own submissions (GET /api/papers/getPapersByAuthor)
✅ Upload revision (POST /api/papers/:paperId/revision)
✅ Version history (GET /api/papers/:paperId/versions)
✅ Upload payment receipt (POST /api/payments/paper/:paperId/upload-receipt — author only)
✅ Track payment (GET /api/payments/paper/:paperId)
✅ View published article via slug URL (getPaperUrl() helper used in all link locations)
✅ Journal browsing for submission (GET /api/author/getAuthorJournals, /getAuthorJournalIssues/:journalId)
✅ Profile management (PUT /api/profile/updateProfile, GET /api/profile/getProfile)
✅ Password change (PUT /api/profile/change-password)
✅ Submission confirmation email (sendSubmissionConfirmationEmail)
✅ Revision request notification (sendRevisionRequestEmail)
✅ Acceptance notification (sendAcceptanceNoticeEmail)
✅ Publication confirmation (sendPublicationConfirmationEmail)
✅ submitted_at, accepted_at, published_at shown in TrackPaper.tsx
✅ Status timeline (PaperTimeline component via TrackPaper → status-log endpoint)
⚠️ MySubmissions.tsx exists but route commented out in App.tsx


[SECTION 14] PUBLIC PAGES & DISCOVERY
========================================

✅ Landing page (/ — LandingPage.tsx — with top journals, top articles, open submissions, upcoming conferences)
✅ Browse journals (/browse — BrowseJournal.tsx — GET /api/browse/getBrowseData)
✅ Journal detail (/journal/:id — PaperDetail.tsx — shows journal info, papers, editorial board)
✅ Research paper detail (/researchPapers/:paperId — ResearchPaper.tsx)
✅ Article page with slug (/:acronym/:slug — ArticlePage.tsx — resolves via GET /api/browse/article/:acronym/:slug)
✅ Article redirect (/articles/:paperId — ArticleRedirect.tsx — fetches slug, redirects to /:acronym/:slug)
✅ Archive (/archive — Archive.tsx — GET /api/archive, GET /api/archive/filters)
✅ About page (/about — aboutUs.tsx)
✅ FAQ page (/faq — faq.tsx)
✅ Contact page (/contact-us — contactUs.tsx — POST /api/contact)
✅ Apply as reviewer (/apply-reviewer — ApplyReviewer.tsx — POST /api/contact/apply-reviewer)
✅ Accept invitation (/accept-invitation — AcceptInvitation.tsx — GET/POST /api/invitations/verify/:token, /accept/:token)
✅ Editorial board (GET /api/journal/:id/editorial-board — public, no auth)
✅ Author guidelines (GET /api/journal/:id/guidelines — public, no auth)
✅ Home page journals (GET /api/browse/home/journals — public)
✅ Home page publications (GET /api/browse/home/publications — public)
✅ Open journals (GET /api/browse/home/open-journals — public)
✅ Upcoming conferences (GET /api/conferences — public)
✅ Health check (GET /health — public)
⚠️ Journal public page tabs (per CLAUDE.md spec: Journal Home, Editorial Board, OA Policy, Peer Review Policy, Articles) — PaperDetail.tsx handles journal detail but may not have all 5 tabs


[SECTION 15] EMAIL NOTIFICATIONS
===================================

28 email functions found across server/src/utils/emails/:

AUTHENTICATION:
✅ sendOTPEmail — called in otp.service.ts on OTP request
✅ sendPasswordResetEmail — exists in authEmails.ts but NOT wired to any endpoint (no /forgot-password route)

USER / STAFF:
✅ sendWelcomeEmail — called in auth.service.ts on create-staff
✅ sendInvitationEmail — called in invitation.controller.ts on send invitation
✅ sendSubEditorInviteEmail — called in chiefEditor.controller.ts (inviteSubEditor)
✅ sendReviewerInviteEmail — called in subEditor.controller.ts (reviewerInvite)

PAPER SUBMISSION & REVIEW:
✅ sendSubmissionConfirmationEmail — called in paper.service.ts on createPaper
✅ sendEditorAssignmentEmail — called in chiefEditor.controller.ts on assignSubEditor
✅ sendReviewerAssignmentEmail — called in subEditor.controller.ts on assignReviewer
✅ sendReviewerReminderEmail — called in chiefEditor.controller.ts (remindReviewer)
✅ sendReviewSubmissionConfirmationEmail — called in reviewer.controller.ts on submitReview
✅ sendEditorialDecisionEmail — called in chiefEditor.controller.ts on decidePaper
✅ sendRevisionRequestEmail — called on AE/CE decision with revision
✅ sendAcceptanceNoticeEmail — called on CE decide with accepted decision
✅ sendPublicationConfirmationEmail — called in publication.service.ts on publishPaper

PUBLICATION:
⚠️ sendProofAvailabilityEmail — exists in paperEmails.ts but NOT called in any controller
⚠️ sendDOIRegistrationEmail — exists in paperEmails.ts but NOT called in any controller

PAYMENT:
✅ sendInvoiceEmail — called in publisher.controller.ts (sendInvoice)
✅ generateInvoiceEmailHtml — helper used by sendInvoiceEmail
✅ sendPaperPaymentEmail — called in paperPayment.controller.ts on reject
✅ sendJournalExpiryInvoiceEmail — called in owner.controller.ts (sendJournalExpiry) and yearlyEmailCron (COMMENTED OUT)
✅ sendPaymentReminderEmail — called in paperPayment.controller.ts (sendPaymentReminder)
✅ sendReceiptNotificationEmail — called in paperPayment.controller.ts on uploadReceipt
✅ sendPaymentApprovalEmail — called in paperPayment.controller.ts on approve

REVIEWER APPLICATIONS:
✅ sendReviewerApplicationToEditor — called in contact.controller.ts (applyAsReviewer)
✅ sendReviewerApplicationConfirmation — called in contact.controller.ts (applyAsReviewer)
✅ sendRejectionEmailToApplicant — called in chiefEditor.controller.ts (declineApplication)

TOTAL EMAIL FUNCTIONS: 28
TOTAL WIRED TO ENDPOINTS: 26
TOTAL NOT WIRED: 2 (sendPasswordResetEmail, sendProofAvailabilityEmail, sendDOIRegistrationEmail)


[SECTION 16] SEARCH & DISCOVERY
==================================

✅ Keyword suggestions (GET /api/papers/keyword-suggestions — returns existing keywords from papers)
✅ Browse journals with filters (GET /api/browse/getBrowseData — supports filtering)
✅ Archive with filters (GET /api/archive — GET /api/archive/filters)
✅ Home page journals feed (GET /api/browse/home/journals)
✅ Home page publications feed (GET /api/browse/home/publications)
✅ Open journal discovery (GET /api/browse/home/open-journals)
✅ Get paper by slug (GET /api/browse/article/:acronym/:slug)
✅ Get paper slug by ID (GET /api/browse/paper/:paperId/slug)
✅ Journal categories (GET /api/journal-categories — public, with journal counts)
✅ Paper categories (paper_categories table exists, GET /api/categories — public)
✅ Editorial board public (GET /api/journal/:id/editorial-board — no auth required)
⚠️ Full-text search across paper titles/abstracts not implemented
⚠️ Author name search not implemented
⚠️ Advanced filter by year/subject in LandingPage described in spec but filtering depth unclear


[SECTION 17] SECURITY
=======================

✅ JWT authentication (authMiddleware validates access token on all protected routes)
✅ Role-based authorization (authorize() middleware enforces per-endpoint role checks)
✅ Password hashing (bcrypt via SALT_ROUND env — applied in auth.service.ts)
✅ OTP verification (6-digit time-limited codes — 10 min expiry)
✅ Refresh token rotation (POST /api/auth/token — stored in refresh_token table)
✅ Logout with token invalidation (POST /api/auth/logout)
✅ Request validation (validate middleware + Zod schemas on all write endpoints)
✅ File type validation (manuscriptUpload: .docx/.tex only; receiptUpload; logoUpload; certificationUpload)
✅ File size limits (manuscriptUpload: 10MB; receiptUpload; logos; profilePic: 2MB)
✅ CORS enabled (cors middleware in app.ts — uses CORS_ORIGIN env)
✅ Cookie parser (cookieParser for refresh token httpOnly cookie)
✅ Parameterized SQL queries throughout (pg pool — no raw string interpolation)
⚠️ No rate limiting middleware found (express-rate-limit not in use)
⚠️ No helmet middleware found (HTTP security headers not explicitly set)
❌ PUT /api/publisher/approve/:paymentId — NO authMiddleware, NO authorize() (critical security bug)
❌ GET /api/chiefEditor/getPapersByIssue/:issueId — NO auth guard
❌ POST /api/chiefEditor/assignPaperToIssue — NO auth guard
❌ PUT /api/chiefEditor/updateIssueStatus/:issueId — NO auth guard
⚠️ Static file serving at /api/uploads/ with no access control (all uploaded files publicly accessible by URL)
⚠️ POST /api/publisher/sendEmail — only authMiddleware, no authorize() role check
⚠️ GET /api/publisher/papers/:issueId — only authMiddleware, no authorize() role check


[SECTION 18] MULTI-ROLE SYSTEM
=================================

✅ user_roles table (migration 015 — user_id, role, journal_id, granted_by, is_active, UNIQUE(user_id, role, journal_id))
✅ Users table retains primary role field (not removed — additive system)
✅ JWT payload includes { sub, active_role, active_journal_id, roles[] }
✅ Role switch endpoint (POST /api/auth/switch-role — validates user_roles, returns new JWT)
✅ Role hierarchy enforcement in switch-role (validates requested role is in user's user_roles)
✅ Per-journal role scoping (journal_id in user_roles allows journal-specific roles)
✅ granted_by tracking (UUID FK to users)
✅ is_active boolean (can deactivate roles without deleting)
✅ Multi-role JWT includes all user roles in roles[] array
✅ Frontend role switcher (roles.ts roleConfig defines per-role nav; switcher visible when user.roles.length > 1)

Supported user_role enum values:
  owner, publisher, journal_manager, chief_editor, sub_editor, reviewer, author
  (note: 'publisher_manager' renamed to 'journal_manager' via migration 014)


[SECTION 19] CRON JOBS
=========================

✅ invitationExpiryCron — ACTIVE (imported and called in server.ts)
   Schedule: 0 * * * * (every hour)
   Function: Expires pending staff_invitations past expiry_date

✅ issueResetCron — ACTIVE (imported and called in server.ts)
   Schedule: 1 0 1 1 * (Jan 1 at 00:01)
   Function: Closes all open journal issues for new year reset
   Manual trigger: POST /api/publisher/issues/reset-all

⚠️ journalSuspensionCron — COMMENTED OUT in server.ts (// journalSuspensionCron())
   Schedule: * * * * * (every minute — suspiciously aggressive)
   Function: Auto-suspends journals past expiry_at without successful renewal
   Impact: Journals never auto-suspended even if expired

⚠️ yearlyEmailCron — COMMENTED OUT in server.ts (// yearlyEmailCron())
   Schedule: 0 0 * * * (daily at midnight)
   Function: Sends renewal invoices to journals expiring today
   Impact: No automated renewal reminder emails sent


[SECTION 20] DATABASE TABLES
==============================

Tables found across 48 migration files (001-048):

✅ users — migration 003 — user accounts, role, status, email, password
✅ refresh_token — migration 004 — JWT refresh tokens per user
✅ journals — migrations 005, 016, 020, 022, 040, 043 — journal records, metadata, policies
✅ journal_issues — migrations 007, 043 — issues per journal, status, volume/issue/year
✅ papers — migrations 008, 018, 025, 026, 030, 031, 034, 037, 043, 045, 046 — submitted papers
✅ paper_assignments (editor_assignments) — migration 009 — paper-to-sub_editor assignments
✅ publications — migrations 010, 019, 023, 047 — published papers with DOI, url_slug, paper_index
✅ otp — migration 011 — one-time passwords
✅ journal_payments — migration 012 — journal subscription/renewal payments
✅ paper_payments — migration 013 — article processing charges
✅ user_roles — migration 015 — multi-role per user per journal
✅ paper_versions — migrations (alter 021) — paper version history with html_content
✅ issue_requests — migration 024, 036 — JM to Publisher issue requests
✅ reviewer_requests — migration 027 — AE to CE reviewer suggestions
✅ staff_invitations — migration 029 — invitation tokens for editors/reviewers
✅ reviewer_applications — migration 033 — reviewer applications from contact form
✅ user_certifications — migration 035 — uploaded certifications per user
✅ paper_categories — migration 037 — paper-to-category mappings
✅ journal_categories — migration 040 — journal category groupings
✅ sub_editor_decisions — migration 041 — AE decisions with sub_editor_id FK
✅ paper_reminders — migration 044 — reminder tracking (24hr rate limit)
✅ paper_status_log — migration (in migrations) — audit trail of paper status changes
✅ editor_assignments — migrations 009, 042 — paper-to-sub_editor with unique constraint fix
✅ review_assignments — (in migrations) — paper-to-reviewer assignments
✅ reviews — (in migrations) — completed reviewer feedback
✅ editor_decisions — (in migrations) — CE final decisions
✅ user_profiles — migration 017 — extended user info (degrees, keywords, profile_pic_url)
✅ conferences — migration 048 — upcoming conferences for homepage

DB ENUMS FOUND:
✅ user_role: owner, publisher, journal_manager, chief_editor, sub_editor, reviewer, author
✅ paper_status: submitted, assigned, under_review, pending_review, pending_revision, resubmitted, accepted, published, rejected, awaiting_payment, payment_review, reviewed, sub_editor_approved
✅ user_status: active, inactive, suspended
✅ issue_status: open, closed, draft, published
✅ journal_status: active, suspended, inactive
✅ review_assignment_status: pending, accepted, declined, completed
✅ editor_assignment_status: pending, accepted, declined
✅ review_decision: accept, reject, revision
✅ chief_editor_decision: accepted, rejected, revision_needed
✅ journal_payment_type: renewal, new_subscription

TOTAL DB TABLES: 28
TOTAL MIGRATIONS: 48 SQL files


[SECTION 21] FRONTEND ROUTES
===============================

PUBLIC ROUTES (wrapped in <PublicRoute>):
✅ / — LandingPage (public)
✅ /login — LoginPage (public)
✅ /signup — SignupPage (public)
✅ /about — AboutPage (public)
✅ /faq — FAQPage (public)
✅ /contact-us — ContactPage (public)
✅ /browse — BrowseJournal (public)
✅ /journal/:id — JournalDetail / PaperDetail.tsx (public)
✅ /researchPapers/:paperId — ResearchPaperDetail (public)
✅ /articles/:paperId — ArticleRedirect (public, backward compat)
✅ /:acronym/:slug — ArticlePage (public, new URL format)
✅ /archive — Archive (public)
✅ /apply-reviewer — ApplyReviewer (public)

OUTSIDE PUBLIC/PROTECTED (special):
✅ /accept-invitation — AcceptInvitation (accessible to both auth and unauth users)
✅ /unauthorized — Unauthorized page
✅ /initialCheckout — InitialAuthCheck

PROTECTED: author, chief_editor, sub_editor, reviewer, publisher, journal_manager, owner:
✅ /author — AuthorDashboard
✅ /author/submit — SubmitPaper
✅ /author/version — PaperVersions
✅ /author/track/:paperId — TrackPaper

PROTECTED: reviewer, chief_editor:
✅ /reviewer — ReviewerDashboard
✅ /reviewer/completed — CompletedReview
✅ /reviewer/completed/:paperId — ReviewDetail

PROTECTED: sub_editor, chief_editor:
✅ /sub-editor — SubEditorDashboard
✅ /sub-editor/revision — RevisionPaper

PROTECTED: chief_editor:
✅ /chief-editor — ChiefEditorDashboard
✅ /chief-editor/accepted — ReviewedPapers
✅ /chief-editor/journals — CEJournals
✅ /chief-editor/journals/:journalId — CEJournalDetail
✅ /chief-editor/papers — CEPapers
✅ /chief-editor/team — CETeam
✅ /chief-editor/applications — CEApplications
✅ /chief-editor/stats — CEStats

PROTECTED: publisher:
✅ /publisher — PublisherDashboard
✅ /publisher/create-journal — CreateJournal
✅ /publisher/publish-paper — PublishPapers
✅ /publisher/payments — PublisherPayments
✅ /publisher/categories — PublisherCategories
✅ /publisher/journal-categories — PublisherJournalCategories
✅ /publisher/homepage-content — HomepageContent

PROTECTED: journal_manager:
✅ /publisher-manager — PublisherManagerDashborad

PROTECTED: owner:
✅ /owner — OwnerDashboard
✅ /journals — Journals

OPEN (no ProtectedRoute wrapper):
⚠️ /admin — AdminDashboard (no ProtectedRoute — accessible without auth)
⚠️ /admin/* — AdminDashboard (no ProtectedRoute)

ANY AUTHENTICATED:
✅ /profile — ProfilePage

CATCH-ALL:
✅ * — NotFound

TOTAL FRONTEND ROUTES: 47 (44 distinct paths)
COMMENTED OUT: /my-submissions (MySubmissions.tsx)


[SECTION 22] API ENDPOINTS SUMMARY
=====================================

AUTH (/api/auth):
  POST /signup — register new user
  POST /login — login with credentials
  POST /create — request signup OTP
  POST /verifysignup — verify OTP, create account
  POST /verifyLoginOTP — verify login OTP, issue JWT
  POST /resend — resend OTP
  POST /token — refresh JWT using cookie
  POST /logout — invalidate refresh token
  POST /switch-role — switch active role, return new JWT
  POST /create-staff — create staff user (CE, publisher, JM, AE)

AUTHOR (/api/author):
  GET /getAuthorJournals — list journals with open issues (author)
  GET /getAuthorJournalIssues/:journalId — list open issues for journal (author)

PROFILE (/api/profile):
  GET /getProfile — get own profile (auth)
  PUT /change-password — change password (auth)
  PUT /updateProfile — update profile + photo upload (auth)
  DELETE /deleteProfile — delete own profile (auth)
  POST /certifications — upload certification file (auth)
  GET /certifications — list own certifications (auth)
  DELETE /certifications/:certId — delete certification (auth)

PAPERS (/api/papers):
  GET /keyword-suggestions — tag suggestions (auth)
  POST /extract-metadata — extract metadata from uploaded file (auth)
  POST /createPaper — submit new paper (auth)
  PATCH /:paperId/assign-issue — assign to issue (journal_manager, chief_editor)
  GET /:paperId/versions — version list (auth)
  GET /getAllPapers — all papers (auth)
  GET /getPapersByAuthor — author's papers (auth)
  PUT /updatePaperStatus/:paperId — update status (editor, owner, admin) [BUG: invalid role names]
  GET /:paperId/tracking — tracking info (auth)
  POST /:paperId/revision — upload revision (auth)
  GET /:paperId/html — render HTML (sub_editor, reviewer, chief_editor, author, publisher)
  GET /:paperId/metadata-check — pre-publish check (publisher, chief_editor, journal_manager)
  GET /:paperId/suggest-doi — suggest DOI (publisher)
  GET /:paperId/status-log — status audit log (auth)

PAPER VERSIONS (/api/paper-versions):
  POST /uploadRevision — upload revision (auth)
  GET /getPaperVersions/:paperId — version list (auth)

JOURNAL (/api/journal):
  POST /addJournal — create journal (owner)
  GET /getOwnerJournal/:id — get owner's journal (owner)
  GET /getJournals — list all journals (public)
  GET /getJournal/:id — get journal by ID (public)
  PUT /updateJournal/:id — update journal (owner)
  DELETE /deleteJournal/:id — delete journal (owner)
  POST /publisherCreate — create journal as publisher (publisher)
  POST /:id/logo — upload journal logo (publisher, journal_manager)
  PATCH /:journalId/apc — set APC fee (publisher)
  GET /:id/editorial-board — editorial board (public)
  GET /:id/guidelines — author guidelines (public)

JOURNAL ISSUES (/api/journal-issue):
  POST /addJournalIssue/:journalId — create issue (owner, publisher, journal_manager)
  GET /:journalId/next-issue-preview — preview next issue number
  GET /getJournalIssues/:journalId — list issues
  PUT /updateJournalIssue/:issueId — update issue
  DELETE /deleteJournalIssue/:issueId — delete issue
  GET /my-issues — JM's issues (journal_manager)
  GET /my-papers — JM's papers (journal_manager)
  POST /request — request new issue (journal_manager)
  GET /:journalId/requests — JM's issue requests (journal_manager)
  GET /pending-requests — pending requests (publisher, owner)
  PUT /requests/:requestId/review — approve/reject request (publisher, owner)

PUBLISHER (/api/publisher):
  PUT /approveJournal/:journalId — approve journal (publisher)
  GET /getJournals — list publisher's journals (publisher)
  PATCH /journals/:journalId/chief-editor — replace CE (publisher)
  GET /getIssues/:journalId — list journal issues (publisher)
  POST /sendInvoice — send invoice email (publisher)
  PUT /publishIssue/:issueId — publish issue (publisher)
  GET /getPapers/:journalId — list papers for journal (publisher)
  GET /papers/:issueId — list papers for issue (authMiddleware only — no authorize)
  POST /sendEmail — send payment email (authMiddleware only — no authorize)
  PUT /approve/:paymentId — approve paper (NO AUTH AT ALL — critical bug)
  GET /getJournalPayments — list journal payments (publisher)
  PUT /updatePaymentStatus/:id — update payment status (publisher)
  POST /issues/reset-all — manual issue reset (publisher)
  POST /journals/:journalId/takedown — take down journal (publisher)
  POST /journals/:journalId/restore — restore journal (publisher)
  POST /journals/:journalId/issues/:issueId/takedown — take down issue (publisher)
  POST /journals/:journalId/issues/:issueId/restore — restore issue (publisher)
  POST /papers/:paperId/takedown — take down paper (publisher)
  POST /papers/:paperId/restore — restore paper (publisher)

PUBLICATION (/api/publication):
  GET /getSubmittedReviews — reviewed papers (journal_manager, publisher)
  PUT /publishPaper/:paperId — publish paper (publisher)

PAYMENTS (/api/payments):
  POST /paper/:paperId/upload-receipt — upload receipt (author)
  POST /paper/:paperId/approve — approve/reject receipt (publisher)
  GET /paper/:paperId — get payment for paper (auth)
  GET /pending — pending receipts (publisher)
  GET /all — all payments (publisher)
  GET /rejected — rejected payments (publisher)
  POST /paper/:paperId/remind — send reminder (publisher)

REVIEWER (/api/reviewer):
  GET /getReviewerPapers — assigned papers (reviewer)
  POST /submitReview/:paperId — submit review with signature (reviewer)

REVIEW (/api/review):
  POST /submitReview/:reviewAssignmentId — submit review (reviewer)

REVIEW ASSIGNMENT (/api/reviewAssignment):
  GET /getSubEditorAssignments — AE's assignments (sub_editor)

EDITOR ASSIGNMENT (/api/editorAssignment):
  GET /getReviews — AE's reviews (sub_editor)
  PUT /handleAssignmentStatus/:editorAssignmentId — accept/decline assignment (sub_editor)

CHIEF EDITOR (/api/chiefEditor):
  GET /getChiefEditorJournals — CE's journals (chief_editor)
  GET /getPapers/:journalId — papers by journal (chief_editor)
  GET /getChiefEditors — list CEs (author, admin, owner, publisher)
  GET /getAllPapers — all papers for CE (chief_editor)
  GET /getSubEditors — list AEs (author, chief_editor, owner, publisher)
  GET /getReviewers — list reviewers (chief_editor, sub_editor)
  POST /assignSubEditor/:paperId — assign AE (chief_editor)
  POST /decide/:paperId — final decision (chief_editor)
  PUT /updatePaperStatus/:paperId — update status (chief_editor)
  GET /getSubmittedReviews — submitted reviews (chief_editor)
  POST /inviteSubEditor — invite AE (chief_editor)
  GET /journals/:journalId/details — journal details (chief_editor)
  GET /getPapersByIssue/:issueId — papers by issue (NO AUTH)
  POST /assignPaperToIssue — assign paper to issue (NO AUTH)
  PUT /updateIssueStatus/:issueId — open/close issue (NO AUTH)
  POST /papers/:paperId/replace-ae — replace AE (chief_editor)
  GET /papers/:paperId/decision-history — decision history (CE, AE, author)
  GET /stats — CE statistics (chief_editor)
  POST /papers/:paperId/override-status — override status (chief_editor)
  POST /papers/:paperId/remind-ae — remind AE (chief_editor)
  POST /papers/:paperId/remind-reviewer/:reviewerId — remind specific reviewer (CE)
  POST /papers/:paperId/remind-reviewer — remind all reviewers (chief_editor)
  POST /ae/:aeId/remind — bulk remind AE (chief_editor)
  POST /reviewer/:reviewerId/remind — bulk remind reviewer (chief_editor)
  GET /applications — reviewer applications (chief_editor)
  GET /applications/count — applications count (chief_editor)
  POST /applications/:applicationId/invite — invite applicant (chief_editor)
  POST /applications/:applicationId/decline — decline applicant (chief_editor)

SUB EDITOR (/api/subEditor):
  GET /getSubEditorPapers — AE's papers (sub_editor)
  GET /fetchReviewer — search reviewers (sub_editor)
  POST /assignReviewer/:paperId — assign reviewer (sub_editor, chief_editor)
  PUT /updateSubEditorPaperStatus/:paperId — update status (sub_editor)
  GET /getReviewersForPaper/:paperId — reviewers for paper (sub_editor, chief_editor)
  POST /inviteReviewer — invite reviewer (sub_editor)
  GET /getReviewsForPaper/:paperId — reviews for paper (sub_editor, chief_editor)
  POST /decision/:paperId — AE decision (sub_editor)
  GET /existingDecision/:paperId — check existing decision (sub_editor)
  POST /suggestReviewer/:paperId — suggest reviewer (sub_editor)
  GET /pending-reviewer-requests — pending suggestions (chief_editor)
  PUT /reviewer-requests/:requestId/review — approve/reject suggestion (chief_editor)

OWNER (/api/owner):
  GET /getPublishers — list publishers (admin, owner)
  GET /getChief-Editor — list CEs (admin, owner)
  POST /createChiefEditor — create CE (owner)
  POST /sendJournalExpiry/:journalId — send expiry email (owner)
  POST /uploadpaymentImage/:id — upload payment image (no auth)
  GET /getPendingJournalPayment/:journalId — pending journal payment (auth)

INVITATIONS (/api/invitations):
  POST /send — send invitation (publisher, chief_editor, sub_editor, owner)
  GET /verify/:token — verify token (public)
  POST /accept/:token — accept invitation (public)
  DELETE /:invitationId/cancel — cancel invitation (publisher, chief_editor, owner)
  GET /journal/:journalId — list journal invitations (publisher, chief_editor, owner)
  POST /resend — resend invitation (no auth guard)

BROWSE (/api/browse):
  GET /getBrowseData — journal browser data (public)
  GET /home/journals — home page journals (public)
  GET /home/publications — home page publications (public)
  GET /home/open-journals — open journals (public)
  GET /article/:acronym/:slug — paper by slug (public)
  GET /paper/:paperId/slug — get slug for paper (public)
  GET /paper/:paperId/html — paper HTML (public)
  GET /paper/:paperId — paper detail (public)

ARCHIVE (/api/archive):
  GET / — archive list with filters (public)
  GET /filters — available filter values (public)

CATEGORIES (/api/categories):
  GET / — list categories (public)
  POST / — create category (publisher)
  DELETE /:id — delete category (publisher)

JOURNAL CATEGORIES (/api/journal-categories):
  GET / — list journal categories (public)
  POST / — create journal category (publisher)
  PATCH /:id — update journal category (publisher)
  DELETE /:id — delete journal category (publisher)

CONTACT (/api/contact):
  POST / — send contact message (public)
  POST /apply-reviewer — apply as reviewer with profile pic (public)

CONFERENCES (/api/conferences):
  GET / — list upcoming conferences (public)
  POST / — create conference (publisher)
  DELETE /:id — delete conference (publisher)

TOTAL API ENDPOINTS: 131 distinct routes across 25 route modules


================================================
SUMMARY COUNTS
================================================
Total ✅ DONE items: ~220
Total ⚠️ PARTIAL items: 24
Total ❌ MISSING / BROKEN items: 9
Total DB tables: 28
Total DB enums: 10
Total DB migrations: 48
Total API endpoints: 131
Total frontend routes: 47
Total email functions: 28 (26 wired, 2 unwired)
Total cron jobs: 4 (2 ACTIVE, 2 COMMENTED OUT)


================================================
ITEMS STILL INCOMPLETE OR BROKEN
================================================

⚠️ PARTIAL (need completion):

1. Password reset flow — sendPasswordResetEmail exists but no /forgot-password or /reset-password endpoint in auth.route.ts
2. sendDOIRegistrationEmail — function exists in paperEmails.ts but no controller calls it; authors never notified of DOI assignment
3. sendProofAvailabilityEmail — function exists but no controller calls it; authors never notified of proof readiness
4. Journal renewal payment frontend — journal_payments table + sendJournalExpiryInvoiceEmail exist but no /publisher/journal-renewals page
5. yearlyEmailCron — COMMENTED OUT; no automated renewal invoice emails sent
6. journalSuspensionCron — COMMENTED OUT; journals not auto-suspended at expiry
7. journalSuspensionCron schedule is "* * * * *" (every minute) — if re-enabled, this will cause extreme DB load; should be "0 0 * * *" or similar
8. JM cannot assign paper to issue from UI — PATCH /api/papers/:paperId/assign-issue allows journal_manager but no UI button exists in PublisherManagerDashborad.tsx
9. Issue request email notification — POST /api/journal-issue/request inserts to DB but no email is sent to publisher notifying them of a new request
10. MySubmissions.tsx — exists in pages/author/ but route is commented out in App.tsx
11. /admin and /admin/* routes — AdminDashboard has no ProtectedRoute wrapper; accessible by any user
12. POST /api/invitations/resend — no auth guard on this endpoint
13. POST /api/owner/uploadpaymentImage/:id — no auth guard
14. Journal public page tabs — per CLAUDE.md spec requires 5 tabs (Journal Home, Editorial Board, OA Policy, Peer Review Policy, Articles); PaperDetail.tsx may not implement all tabs
15. GET /api/chiefEditor/getChiefEditors — allows "author" role which seems overly permissive
16. GET /api/chiefEditor/getSubEditors — allows "author" role which seems overly permissive


================================================
KNOWN BUGS / RUNTIME ERRORS
================================================

CRITICAL SECURITY BUGS:
1. PUT /api/publisher/approve/:paymentId (publisher.route.ts line 75)
   — NO authMiddleware, NO authorize() — completely public endpoint
   — Anyone on the internet can approve or reject paper payments
   — Fix: add authMiddleware + authorize("publisher")

2. GET /api/chiefEditor/getPapersByIssue/:issueId (chiefEditor.routes.ts line 129)
   — NO auth guard — router.get("/getPapersByIssue/:issueId", getPapersByIssue)
   — Fix: add authMiddleware + authorize("chief_editor", "publisher")

3. POST /api/chiefEditor/assignPaperToIssue (chiefEditor.routes.ts line 131)
   — NO auth guard — router.post("/assignPaperToIssue", assignPaperToIssue)
   — Fix: add authMiddleware + authorize("chief_editor")

4. PUT /api/chiefEditor/updateIssueStatus/:issueId (chiefEditor.routes.ts line 133)
   — NO auth guard — router.put("/updateIssueStatus/:issueId", updateIssueStatus)
   — Fix: add authMiddleware + authorize("chief_editor")

POTENTIAL OPERATIONAL ISSUES:
5. journalSuspensionCron schedule "* * * * *" (every minute) — if uncommented it will run a DB query every minute checking for expired journals; this should likely be "0 0 * * *" (daily at midnight)

6. PUT /api/papers/updatePaperStatus/:paperId uses authorize("editor", "admin") — neither "editor" nor "admin" are valid values in the user_role enum; this endpoint is effectively inaccessible to all users

7. Static file route order in app.ts: /api/uploads/receipts and /api/uploads/profiles are registered AFTER /api/uploads which catches all paths first — the specific sub-routes may never be reached; verify static middleware order

8. PaymentModal.tsx in client/src/components/ may be an empty or stub component; verify it is not imported and expected to render

9. /admin and /admin/* routes in App.tsx have NO ProtectedRoute wrapper — AdminDashboard accessible without authentication

10. POST /api/auth/switch-role with Zod schema validation — if user's roles[] in JWT is empty or missing, the schema may reject valid requests; confirm switchRoleSchema handles optional roles[]


================================================
END OF REPORT
================================================
